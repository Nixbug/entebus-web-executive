/* tslint:disable */
/* eslint-disable */
/**
 * Executive APP
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AppApiExecutiveTokenOrderBy,
  AppApiVendorRoleMapOrderIn,
  ErrorResponse,
  ExecutiveTokenSchema,
  HTTPValidationError,
  MaskedExecutiveTokenSchema,
  PlatformType,
} from '../models/index';
import {
    AppApiExecutiveTokenOrderByFromJSON,
    AppApiExecutiveTokenOrderByToJSON,
    AppApiVendorRoleMapOrderInFromJSON,
    AppApiVendorRoleMapOrderInToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ExecutiveTokenSchemaFromJSON,
    ExecutiveTokenSchemaToJSON,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
    MaskedExecutiveTokenSchemaFromJSON,
    MaskedExecutiveTokenSchemaToJSON,
    PlatformTypeFromJSON,
    PlatformTypeToJSON,
} from '../models/index';

export interface CreateTokenEntebusAccountTokenPostRequest {
    username: string;
    password: string;
    platformType?: PlatformType;
    clientDetails?: string | null;
}

export interface DeleteTokenEntebusAccountTokenDeleteRequest {
    id?: number | null;
}

export interface FetchTokensEntebusAccountTokenGetRequest {
    executiveId?: number | null;
    platformType?: PlatformType | null;
    clientDetails?: string | null;
    id?: number | null;
    idGe?: number | null;
    idLe?: number | null;
    idList?: Array<number> | null;
    updatedOnGe?: Date | null;
    updatedOnLe?: Date | null;
    createdOnGe?: Date | null;
    createdOnLe?: Date | null;
    orderBy?: AppApiExecutiveTokenOrderBy;
    orderIn?: AppApiVendorRoleMapOrderIn;
    offset?: number;
    limit?: number;
}

export interface RefreshTokenEntebusAccountTokenPatchRequest {
    id?: number | null;
}

/**
 * 
 */
export class TokenApi extends runtime.BaseAPI {

    /**
     * Issues a new access token for an executive after validating credentials.         If the credentials are valid and the executive account is active, a new token is generated and returned.         Limits active tokens using MAX_EXECUTIVE_TOKENS (token rotation).        Sets expiration with expires_in=MAX_TOKEN_VALIDITY (in seconds).         Logs the authentication event for audit tracking.
     * Create Token
     */
    async createTokenEntebusAccountTokenPostRaw(requestParameters: CreateTokenEntebusAccountTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExecutiveTokenSchema>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling createTokenEntebusAccountTokenPost().'
            );
        }

        if (requestParameters['password'] == null) {
            throw new runtime.RequiredError(
                'password',
                'Required parameter "password" was null or undefined when calling createTokenEntebusAccountTokenPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['username'] != null) {
            formParams.append('username', requestParameters['username'] as any);
        }

        if (requestParameters['password'] != null) {
            formParams.append('password', requestParameters['password'] as any);
        }

        if (requestParameters['platformType'] != null) {
            formParams.append('platform_type', requestParameters['platformType'] as any);
        }

        if (requestParameters['clientDetails'] != null) {
            formParams.append('client_details', requestParameters['clientDetails'] as any);
        }


        let urlPath = `/entebus/account/token`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExecutiveTokenSchemaFromJSON(jsonValue));
    }

    /**
     * Issues a new access token for an executive after validating credentials.         If the credentials are valid and the executive account is active, a new token is generated and returned.         Limits active tokens using MAX_EXECUTIVE_TOKENS (token rotation).        Sets expiration with expires_in=MAX_TOKEN_VALIDITY (in seconds).         Logs the authentication event for audit tracking.
     * Create Token
     */
    async createTokenEntebusAccountTokenPost(requestParameters: CreateTokenEntebusAccountTokenPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExecutiveTokenSchema> {
        const response = await this.createTokenEntebusAccountTokenPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Revokes an active access token associated with an executive account.         This endpoint deletes an access token based on the token ID (optional).      If no ID is provided, it deletes the token used in the request (self-revocation).        If an ID is provided, the caller must either, own the token being deleted, or have a role with `manage_ex_token` permission.      If the token ID is invalid or already deleted, the operation is silently ignored.        Logs the token revocation event for audit tracking.
     * Delete Token
     */
    async deleteTokenEntebusAccountTokenDeleteRaw(requestParameters: DeleteTokenEntebusAccountTokenDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("Executive HTTPBearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['id'] != null) {
            formParams.append('id', requestParameters['id'] as any);
        }


        let urlPath = `/entebus/account/token`;

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Revokes an active access token associated with an executive account.         This endpoint deletes an access token based on the token ID (optional).      If no ID is provided, it deletes the token used in the request (self-revocation).        If an ID is provided, the caller must either, own the token being deleted, or have a role with `manage_ex_token` permission.      If the token ID is invalid or already deleted, the operation is silently ignored.        Logs the token revocation event for audit tracking.
     * Delete Token
     */
    async deleteTokenEntebusAccountTokenDelete(requestParameters: DeleteTokenEntebusAccountTokenDeleteRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTokenEntebusAccountTokenDeleteRaw(requestParameters, initOverrides);
    }

    /**
     * Retrieves access tokens associated with executive accounts.          If the authenticated user has the `manage_ex_token` permission, all masked tokens from the ExecutiveToken table are returned.          If the authenticated user don\'t have manage_ex_token permission, only their own masked tokens are returned.          Returns a list of masked token data, excluding access_token content.         Useful for reviewing active or historical token usage management.
     * Fetch Tokens
     */
    async fetchTokensEntebusAccountTokenGetRaw(requestParameters: FetchTokensEntebusAccountTokenGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<MaskedExecutiveTokenSchema>>> {
        const queryParameters: any = {};

        if (requestParameters['executiveId'] != null) {
            queryParameters['executive_id'] = requestParameters['executiveId'];
        }

        if (requestParameters['platformType'] != null) {
            queryParameters['platform_type'] = requestParameters['platformType'];
        }

        if (requestParameters['clientDetails'] != null) {
            queryParameters['client_details'] = requestParameters['clientDetails'];
        }

        if (requestParameters['id'] != null) {
            queryParameters['id'] = requestParameters['id'];
        }

        if (requestParameters['idGe'] != null) {
            queryParameters['id_ge'] = requestParameters['idGe'];
        }

        if (requestParameters['idLe'] != null) {
            queryParameters['id_le'] = requestParameters['idLe'];
        }

        if (requestParameters['idList'] != null) {
            queryParameters['id_list'] = requestParameters['idList'];
        }

        if (requestParameters['updatedOnGe'] != null) {
            queryParameters['updated_on_ge'] = (requestParameters['updatedOnGe'] as any).toISOString();
        }

        if (requestParameters['updatedOnLe'] != null) {
            queryParameters['updated_on_le'] = (requestParameters['updatedOnLe'] as any).toISOString();
        }

        if (requestParameters['createdOnGe'] != null) {
            queryParameters['created_on_ge'] = (requestParameters['createdOnGe'] as any).toISOString();
        }

        if (requestParameters['createdOnLe'] != null) {
            queryParameters['created_on_le'] = (requestParameters['createdOnLe'] as any).toISOString();
        }

        if (requestParameters['orderBy'] != null) {
            queryParameters['order_by'] = requestParameters['orderBy'];
        }

        if (requestParameters['orderIn'] != null) {
            queryParameters['order_in'] = requestParameters['orderIn'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("Executive HTTPBearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entebus/account/token`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(MaskedExecutiveTokenSchemaFromJSON));
    }

    /**
     * Retrieves access tokens associated with executive accounts.          If the authenticated user has the `manage_ex_token` permission, all masked tokens from the ExecutiveToken table are returned.          If the authenticated user don\'t have manage_ex_token permission, only their own masked tokens are returned.          Returns a list of masked token data, excluding access_token content.         Useful for reviewing active or historical token usage management.
     * Fetch Tokens
     */
    async fetchTokensEntebusAccountTokenGet(requestParameters: FetchTokensEntebusAccountTokenGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<MaskedExecutiveTokenSchema>> {
        const response = await this.fetchTokensEntebusAccountTokenGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Refreshes an existing executive access token.     If no id is provided, refreshes only the current token (used in this request).       If an id is provided: Must match the current token\'s access_token (prevents unauthorized refreshes, even by the same executive).         Raises InvalidIdentifier if the token does not exist (avoids ID probing).        Extends expires_at by MAX_TOKEN_VALIDITY seconds.        Rotates the access_token value (invalidates the old token immediately).      Logs the refresh event for auditability.
     * Refresh Token
     */
    async refreshTokenEntebusAccountTokenPatchRaw(requestParameters: RefreshTokenEntebusAccountTokenPatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExecutiveTokenSchema>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("Executive HTTPBearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['id'] != null) {
            formParams.append('id', requestParameters['id'] as any);
        }


        let urlPath = `/entebus/account/token`;

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExecutiveTokenSchemaFromJSON(jsonValue));
    }

    /**
     * Refreshes an existing executive access token.     If no id is provided, refreshes only the current token (used in this request).       If an id is provided: Must match the current token\'s access_token (prevents unauthorized refreshes, even by the same executive).         Raises InvalidIdentifier if the token does not exist (avoids ID probing).        Extends expires_at by MAX_TOKEN_VALIDITY seconds.        Rotates the access_token value (invalidates the old token immediately).      Logs the refresh event for auditability.
     * Refresh Token
     */
    async refreshTokenEntebusAccountTokenPatch(requestParameters: RefreshTokenEntebusAccountTokenPatchRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExecutiveTokenSchema> {
        const response = await this.refreshTokenEntebusAccountTokenPatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
